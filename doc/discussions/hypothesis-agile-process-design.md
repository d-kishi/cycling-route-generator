# 仮説検証型アジャイルプロセスの設計議論

> 市谷聡啓「正しいものを正しく作る」第5章 "仮説検証型アジャイル開発" をClaude Codeとの協同で実現するための設計議論。

## ステータス: 議論中

最終更新: 2026-04-03

---

## 1. 背景と目的

### プロジェクト重要目標（CLAUDE.mdに記載済み）

仮説検証型アジャイル（特に仮説キャンバス）を、Claude Codeとの協同で実践する。製造前の工程（仮説キャンバス → EARS要件定義 → ドメインモデル）をSkills等で仕組み化し、「正しいものを探す」プロセスをAIエージェントと共に回す。

### 核心的なアイデア

- セットベースからポイントベースに絞り込むプロセスを、ペルソナを持たせたAgent Teamsによる議論 + POの判断 + 想定ユーザインタビューで組み立てる
- 個人開発で最も欠けやすい「視座の多様性」をAgent Teamsで補う

---

## 2. 製造前プロセスの全体設計

### フェーズ構成

```
Phase 1: 仮説キャンバス作成
  ├─ 仮説の起点（根源）の議論と決定
  ├─ 初期仮説の立案（PO + 開発チームリーダー対話）
  ├─ ペルソナの設定
  └─ 仮説キャンバスの初期ドラフト作成

Phase 2: 仮説検証（セットベース → ポイントベース）
  制約: Phase 1で定めた根源から大幅に逸脱しない
  ├─ Step 1: 並列レビュー（各ペルソナが独立に評価）
  ├─ Step 2: POの参加（意見追加、対立点の明確化）
  ├─ Step 3: ディベート（対立が生じた論点のみ）
  ├─ Step 4: インタビュー結果の注入（仲間ヒアリング + SNS調査）
  └─ Step 5: MVP境界の決定（POが最終判断、必須/将来拡張/不要に分類）

Phase 3: 要件定義（/spec-requirements）
  ├─ MVP範囲のEARS形式要件化
  └─ ペルソナレビューで要件の妥当性確認

Phase 4: ドメインモデル
  └─ バウンデッドコンテキスト・エンティティ/値オブジェクトの識別

Phase 5: 実装開始（Nx初期化〜）
```

### ドキュメント体系

| レイヤー | ドキュメント | 内容 | 粒度 |
|---|---|---|---|
| 仮説 | doc/hypothesis-validation/hypothesis-canvas.yaml | 仮説キャンバス | Why / For Whom |
| 要件 | doc/requirements.md | EARS形式 + 否定的境界 | What |
| ドメイン | doc/domain-model.md | BC、エンティティ、コンテキストマップ | What/Why（構造） |
| 実装 | コード | interface定義、データモデル等 | How |

---

## 3. 役割分担

### Phase 1: 仮説キャンバス作成

| 役割 | 担当 | 責務 |
|---|---|---|
| プロダクトオーナー（PO） | ユーザ | ビジョン・仮説の起点を提供、最終判断 |
| 開発チームリーダー | メインAgent（Claude） | 技術観点での意見、仮説の構造化支援 |

### Phase 2: 仮説検証

| 役割 | 担当 | 責務 |
|---|---|---|
| プロダクトオーナー（PO） | ユーザ | 議論への参加（中級ホビーサイクリストとして）、最終判断 |
| スクラムマスター/ファシリテータ | メインAgent（Claude） | 議論の促進・整理、技術的実現性の指摘（中立だが技術判断は例外） |
| チームメンバー | ペルソナAgent（Agent Teams） | 各ペルソナの視点から仮説を評価・議論 |

### ファシリテータの行動原則

- 基本姿勢: 中立。自分の意見は混ぜない
- 例外: 技術的に実現困難な場合はPOに伝える（判断材料として）
- ペルソナAgentの意見を収集・整理し、POに分かりやすく提示する
- 対立が生じた論点を特定し、必要に応じてディベートを設計する

---

## 4. ペルソナレビューの設計

### 議論形態: 段階的アプローチ

1. **並列レビュー**: 各ペルソナAgentが独立に意見を出す
2. **ディベート**: 対立が生じた論点のみ、ペルソナAgent同士が議論
3. **POへの提示**: ファシリテータが整理してPOに判断材料を届ける

### ペルソナの設定方針

- プロジェクトのドメインに応じて自由に設定可能（汎用設計）
- スキル起動時にユーザが設定する（ハードコードしない）
- ユーザ自身が該当するペルソナも含める（自分の視点を客観化する機会）
- 推奨: 2-4体

### Agent構成

- ペルソナAgent: subagent_type: general-purpose, model: sonnet
- ファシリテータ: メインAgent（セッションのモデルを使用）

---

## 5. 仮説の根源（アンカー）による制約

### 課題

AIモデルは無限の情報にアクセスできるため、議論が発散しやすい。不確実な結論を出すことが本質的に難しい。

### 解決策

Phase 1で「仮説の根源」を決定し、Phase 2ではこれをアンカーとする。

- 根源は書籍の"どこから仮説を立てるべきか?"に基づき、起点（目的/課題/状況等）を議論して決定する
- Phase 2の全議論は「根源に照らして妥当か」を判断軸とする
- 根源から大幅に逸脱する提案にはフラグを立てる
- このプロジェクトの起点はPhase 1で議論して決定する（現時点では未定）

---

## 6. インタビュー計画

### 対象

- サイクリング仲間への直接ヒアリング（少人数・深い）
- SNS/コミュニティでの調査（Strava、X等。広く浅く）

### 地理的範囲

- まずは東京周辺に絞って検証

### 注入タイミング

- Phase 2のStep 4でフィードバック
- 具体的な形式（メモ、書き起こし等）は実施段階で決定

---

## 7. 仮説キャンバスの表現方法

### 両面アプローチ

- **Agent向け**: YAML形式の構造化文書（読み書きの合理性）
- **人間向け**: Miro MCP経由でビジュアルボードに描画（俯瞰性）

### YAML フォーマット（確定）

`doc/discussions/hypothesis-canvas-format-draft.yaml` にフォーマット案を記録済み。主要な構造:
- `meta`: プロジェクト名、バージョン、フェーズ
- `root`: 仮説の根源（Phase 2のアンカー）。type / statement / rationale
- `canvas`: 仮説キャンバス本体。customer / provider / business の3区画
  - 各仮説: `id`, `title`（一言サマリー）, `hypothesis`, `confidence`, `evidence`
- `relations`: 仮説間のリレーション（from → to, type: supports/derives/conflicts, note）

### ビジュアライズツール: Miro MCP（動作確認済み）

公式Miro MCP（`https://mcp.miro.com`）を採用。セットアップ完了済み。

**Miro選定の根拠:**
- 仮説キャンバスだけでなく、MVP特定後のイベントストーミングにも転用したい
- Miro MCPの今後の機能拡充に期待

**描画テスト結果（2026-04-03）:**
- flowchart DSLで仮説キャンバスを表現可能
- cluster機能で「顧客/ユーザ側」「提供者側」のセクション分けができる
- コネクタでリレーション（supports/derives）を表現
- 色分け: 根源=赤、顧客側=黄、提供者側=青
- テストボード: https://miro.com/app/board/uXjVGoe-c2k=/
- 書籍の構成への近づけ方は実践しながら調整する方針

**公式MCPの提供ツール**: context_explore / context_get / diagram_create / diagram_get_dsl / doc_create / doc_get / doc_update / table_create / table_list_rows / table_sync_rows / board_list_items / image_get_data / image_get_url

**フォールバック**: Miro MCPで不十分な場合、HTML生成（YAML → スタンドアロンHTML）に切り替える。

### 未決定事項

- 仮説キャンバスの14項目をフルで使うか、プロダクト側に絞るか

---

## 8. 既存のSkill: spec-requirements

### ステータス: ドラフト完成済み

`.claude/skills/spec-requirements/SKILL.md` に配置。

### 主要機能

- EARS形式の要件定義書（doc/requirements.md）を生成・修正
- 2階層の否定的境界定義（プロジェクトNon-Goals + 各要件Out of Scope）
- ペルソナレビュー機能（Agent Teamsで並列レビュー、汎用設計）
- 呼び出し: `/spec-requirements`, `/spec-requirements add`, `modify N`, `review`
- model: opus

---

## 9. 未着手のSkill/ツール

| 名称 | 目的 | ステータス |
|---|---|---|
| /spec-hypothesis | 仮説キャンバスの生成・管理 | 構想段階。フォーマット設計が前提 |
| /spec-domain-model | ドメインモデルの生成・管理 | 構想のみ |
| code-reviewer agent | コード品質・セキュリティレビュー | 未着手 |

---

## 10. 将来的なアイデア（未実装）

### /loop コマンドの活用可能性

仮説検証サイクルを「1セッションで完結させない」運用にした場合、`/loop` で定期的に新しいインタビューメモやPOコメントの追加をチェックし、次のdebateラウンドのトリガーを提案するウォッチャーとして活用できる可能性がある。まずはスキルを手動で回し、自動化したいポイントが明確になってから検討する。

### ブロードリスニングによるペルソナ形成の高度化（2026-04-14提案、2026-04-15詳細化）

ペルソナAgentに渡す情報量を増やし、よりリアルなペルソナを形成するためにブロードリスニングを活用する構想。

**アプローチ: A+Bの段階的組み合わせ**
- **A（ペルソナ形成時）**: ブロードリスニングで各ペルソナ層の言葉遣い、関心事、不満、行動パターンを収集し、ペルソナの人格・知識・特性を充実させる
- **B（debate時）**: 議論テーマに関連する情報を追加収集し、リアルな声に基づいた意見をAgentに出させる

#### ペルソナ形成スキル（`/persona`）の設計

**独立したSkill/プラグインとして設計する。** spec-hypothesisの内部機能にしない。仮説検証だけでなく、デザインレビュー、別プロジェクトへの横展開も視野に入れる。

**呼び出しパターン:**

| コマンド | 動作 |
|---|---|
| `/persona` | 新規ペルソナ形成（対話→チェックリスト→ブロードリスニング→形成） |
| `/persona update` | 既存ペルソナの更新 |
| `/persona list` | 形成済みペルソナ一覧の表示 |

**新規ペルソナ形成のワークフロー:**

1. **ユーザとの対話**: ペルソナの内容（属性、特性）とブロードリスニング用チェックリスト（収集すべき情報カテゴリ）を対話しながら形成
   - チェックリストの粒度は初回は粗く、不足時やペルソナの内容次第でLLMが自動的に細分化
2. **ファイルに保存**: ペルソナ内容とチェックリストを所定のディレクトリに保存
3. **SubAgentによるブロードリスニング**: チェックリストをpromptで渡してSubAgentを起動
4. **返却結果でペルソナを充実化**: SubAgentの要約をペルソナに統合
5. **PO承認（オプション）**: デフォルトは自動進行。初期段階では有効化を推奨

**更新時のワークフロー:**
- 既存ペルソナ + 既存チェックリストを読み込み
- 必要に応じてチェックリストをより細かい粒度に拡張
- SubAgentで追加情報収集 → ペルソナ更新

#### ブロードリスニングSubAgentの設計

**SubAgentパターン（Agentパターン = 業務委託モデル）で実装する。**

SubAgentは汎用的な「ブロードリスニングによる情報収集・要約」であり、ドメイン固有の知識は持たない。

**SubAgentの振る舞い:**
- 渡されたチェックリストに沿って情報収集を実施
- 収集した情報の十分性を自律的に評価（Agentパターンの核心）
- 不十分と判断した場合は自ら追加収集を判断・実行
- 自身が十分と判断した時点で要約を返却

**完了条件（客観的に検証可能）:**
- 渡されたチェックリストの全カテゴリについて要約を生成した
- 各カテゴリの充足度評価を付記した
- 情報が見つからなかったカテゴリは「未発見」と明示した

**memoryによる段階的精度強化:**
- SubAgentのmemory機能を使用
- 過去の収集で学んだメタ知識（「この種の情報は見つかりやすい/見つかりにくい」等）を蓄積
- 十分性の評価精度が回を重ねるごとに向上する

**情報の仕切り:**
- ペルソナ別にSubAgentを分離起動するため、情報汚染がアーキテクチャレベルで防止される
- 各SubAgentには自分のペルソナ用チェックリストのみ渡す

**200kコンテキスト制限への対策:**
- 「収集→即座に要約→生データ破棄→次の収集」の逐次処理で対応

#### アーキテクチャ概要

```
ペルソナ形成スキル /persona（メインAgent上で実行）
  ├── ユーザとの対話でペルソナ内容+チェックリスト形成
  ├── ファイル保存
  ├── SubAgent起動（チェックリストをpromptで渡す）
  │     └── 汎用ブロードリスニングSubAgent
  │           ├── 情報収集（自律的）
  │           ├── 十分性評価（自律的）
  │           ├── 追加収集（自律的判断）
  │           ├── memory: 過去知見の蓄積
  │           └── 要約を返却
  ├── 返却結果でペルソナ充実化
  └── PO承認（オプション、デフォルトは自動進行）

        ↓ 形成されたペルソナを利用

spec-hypothesis（仮説キャンバスのdebate）
デザインレビュー（将来）
別プロジェクト（将来）
```

#### Plugin公開の構想

1人のエンジニアの視野を拡大し、多様性のある視点を持ち込める仕組みとして、Claude Codeプラグインとしての公開も視野に入れている。「コードを書くAgent」ではなく「多様な視点を持つペルソナAgent」という希少な価値提供になりうる。

#### 未決定事項

- ブロードリスニングの具体的な情報源（Web検索、特定コミュニティ、SNS等）
- チェックリストとペルソナ情報のファイル配置先
- SubAgentのmemoryスコープ（user / project / local）

---

## 11. 次のアクション

1. ~~仮説キャンバスの構造化フォーマット（yaml/md）の設計~~ → 完了
2. ~~Miro MCPのセットアップと実力検証~~ → 完了（動作確認済み）
3. ~~/spec-hypothesis スキルの設計~~ → 完了（ドラフト作成済み）
4. ~~仮説キャンバスの初回作成~~ → 完了（v1作成済み）
5. **ペルソナ形成スキルの設計**（ブロードリスニング活用、独立Plugin構想）
6. **検証フェーズのSkills作成**（インタビュー・モックアップ関連）
